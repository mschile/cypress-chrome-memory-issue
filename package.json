{
  "name": "cypress-test-tiny",
  "version": "1.0.0",
  "description": "Small project for quickly testing Cypress.io test runner",
  "scripts": {
    "cypress:run": "cypress run --browser chromium --e2e",
    "cypress:run:force:gc": "CYPRESS_FORCE_GC=true npm run cypress:run",
    "cypress:open": "cypress open --browser chromium --e2e",
    "cypress:open:force:gc": "CYPRESS_FORCE_GC=true npm run cypress:open",
    "cypress:verify": "cypress verify",
    "cypress:version": "cypress version",
    "cypress:info": "cypress info",
    "cypress:cache:list": "cypress cache list",
    "list-videos": "ls -l cypress/videos",
    "set-status": "set-status"
  },
  "keywords": [],
  "license": "ISC",
  "devDependencies": {
    "cypress": "^11.1.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/cypress-io/cypress-test-tiny.git"
  }
}
